# Day 12 – Exercises

1. **PDF loader** – Use `PyPDFLoader` to load a PDF file and build RAG over it.
2. **Change chunk size** – Try chunk_size 200, 500, 1000. Which gives best answers?
3. **Add memory** – Use `ConversationBufferMemory` to make the RAG chain multi‑turn.
4. **Switch to local embeddings** – Use `HuggingFaceEmbeddings` instead of OpenAI.