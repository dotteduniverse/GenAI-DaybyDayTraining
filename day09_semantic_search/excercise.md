# Day 9 Exercises

1. **Larger corpus** – Use the 20 Newsgroups dataset (from sklearn) or a book chapter. Index it and test queries.
2. **Precision/recall** – Create a small test set of queries with known relevant documents and compute recall@3.
3. **Interactive app** – Build a Gradio interface that returns the top‑5 sentences for a query.
4. **Different model** – Try `intfloat/e5-small-v2` and compare retrieval quality.