# Day 15 – Exercises

1. **Product extraction** – From “Apple iPhone 14, $999, in stock” → extract name, price, stock status.
2. **SQL generation** – Define a function `run_sql(query)` and ask the model to generate SQL for “show all users over 18”.
3. **Conversation parser** – Given a raw chat log, output JSON with `[{role, message}]`.
4. **Error handling** – If JSON mode fails, retry with a corrected prompt.