# Day 19 – Exercises

1. **Optimise for your own task** – Choose a task (e.g., email subject line generation). Create 10 training examples, use DSPy to optimise.
2. **Compare teleprompters** – Try `BootstrapFewShot`, `LabeledFewShot`, and `MIPRO`.
3. **Local model** – Use DSPy with Ollama (set `dspy.OllamaLocal`).
4. **AutoPrompt with gradient** – Use `transformers` and `torch` to implement a simple gradient‑based trigger search.