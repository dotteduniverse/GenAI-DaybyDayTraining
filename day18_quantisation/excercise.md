# Day 18 – Exercises

1. **Quality comparison** – Generate 20 outputs from FP16 and 4‑bit, compute BERTScore against reference.
2. **Quantise your own model** – Use `optimum` to quantise a fine‑tuned GPT‑2 to INT8.
3. **CPU vs GPU** – Compare speed of GGUF (CPU) vs GPTQ (GPU) on the same prompt.
4. **Extreme quantisation** – Try 2‑bit and see how quality degrades.